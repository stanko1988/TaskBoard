html
  head
    block head
    - var appName = 'Task Board'
    title #{pageTitle} &ndash; #{appName}
    link(rel='stylesheet', href='/styles.css')
    meta(name="viewport", content="width=device-width, initial-scale=1")

  body
    header.top
      a(href='/')
        span &#x1f5b9;
      h1 #{pageTitle}
	  
    aside
      ul
        li
          a(href='/') Home
        li
          a(href='/boards') Task Board
        li 
          a(href='/tasks/create') Create
        li 
          a(href='/tasks/search') Search

    main
      if errMsg
        div(class='err') Error: #{errMsg}
      block content
    
    footer
      div © 2021 - #{appName}
